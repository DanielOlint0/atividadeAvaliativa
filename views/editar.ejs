<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Contato</title>
</head>
<body>
    <h1>Editar Contato</h1>
    <form action="/agenda/editar/<%= contato.id %>" method="post"> 
        <label for="nome">Nome:</label>
        <input type="text" id="nome" name="nome" value="<%= contato.nome %>" required>
        <br>
        <label for="idade">Idade:</label>
        <input type="number" id="idade" name="idade" value="<%= contato.idade %>" required>
        <br>
        <label for="relacionamento">Relacionamento:</label>
        <input type="text" id="relacionamento" name="relacionamento" value="<%= contato.relacionamento %>" required>
        <br>
        <label for="email">Email:</label>
        <input type="email" id="email" name="email" value="<%= contato.email %>" required>
        <br>
        <label for="telefone">Telefone:</label>
        <input type="text" id="telefone" name="telefone" value="<%= contato.telefone %>" required>
        <br>
        <input type="hidden" name="_method" value="PUT">
        <button type="submit">Salvar Alterações</button>
    </form>
</body>
</html>
